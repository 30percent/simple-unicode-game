import { State } from "../state";
export declare function basicEnemyCombat(enemyId: string, state: State): State;
export declare function basicEnemyCombatPlayer(enemyId: string, state: State): State;
